[profile.default]
src = "src"
test = "test"
script = "script"
out = "out"
libs = ["lib"]
solc_version = "0.8.28"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
# Testnet only. This project sends nothing to HyperEVM mainnet (chain 999).
hyperevm_testnet = "https://rpc.hyperliquid-testnet.xyz/evm"
